
<div class="container">
  <div class="row">
  <div class="container-inner inner-default clearfix">
    <% items.each do |item| %>
    <div class="col-sm-3">
      <div class="card">
        <%= image_tag('no-image.png', alt: item['name'], class: "card-img-top") %>
        <div class="card-block">
          <h4 class="card-title"> <%= link_to item.name, '#' %> </h4>
          <% #TODO 仕入れを作成したら残り個数を表示させる %>
          <p class="card-text">残りn個</p>
          <a href="#" class="btn btn-primary">カートに入れる</a>
        </div>
      </div>
    </div>

     <% end %>
  </div>
  </div>
</div>

