<%# 基本的に修正はしない %>
<h1>仕入れ明細</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h2>日時:<%= @purchase.created_at %></h2>
  </div>
  <div class="purchase-item">
    <%= render 'share/item_table', items: @purchase.purchase_items %>
  </div>
</div>


<%= debug @purchase if Rails.env.development? %>